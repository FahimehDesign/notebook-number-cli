# Phone Notebook

A simple Python program for managing contacts. You can add, delete, edit, search, and view all contacts. All data is saved automatically in number.json.

## Features
- Add new contact  
- Delete contact  
- Edit contact (keeps old values if left empty)  
- Show one contact by name  
- Show all contacts (uses enumerate)  
- Saves and loads data from a JSON file  
